{
  "name": "tic_tac_toe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc && copyfiles -u 1 src/**/*.* -e src/**/*.ts  dist/",
    "start-dev": "tsc -w && copyfiles -u 1 src/**/*.* -e src/**/*.ts  dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
